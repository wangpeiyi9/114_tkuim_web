version: '3.9'
services:
  mongodb:
    image: mongo:7
    container_name: restaurant-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: restaurant
    volumes:
      - ./mongo-data:/data/db