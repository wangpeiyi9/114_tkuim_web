# docker/docker-compose.yml
version: '3.9'

services:
  mongodb:
    image: mongo:7
    container_name: restaurant-mongo
    ports:
      - "27017:27017"
    volumes:
      - ./mongo-data:/data/db
    command: mongod --bind_ip_all --quiet